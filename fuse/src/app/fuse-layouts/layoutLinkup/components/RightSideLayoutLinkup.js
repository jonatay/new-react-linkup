import React from 'react';
import QuickPanel from 'app/fuse-layouts/shared-components/quickPanel/QuickPanel';

function RightSideLayoutLinkup(props) {
	return (
		<>
			<QuickPanel />
		</>
	);
}

export default React.memo(RightSideLayoutLinkup);
